Add `.mp4`, `.mov`, or `.webm` video files to this folder.

The invitation page automatically displays them in the Reels section. File names become captions,
so `first-anniversary.mp4` is shown as `First Anniversary`.
